import React from "react";
import "./Header.css";

const Header = () => (
    <header className="header">
        <h1>Clicky Game</h1>
        <h2>Click on all the faces but don't click on the same picture more than once. Click all 12 faces to prove your memory skills.</h2>
    </header>
);

export default Header;